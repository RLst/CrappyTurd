using UnityEngine;

namespace Pokoro
{
	//!Tutorial notes: Use tags first, then come back and use these later
	public class Pipes : MonoBehaviour
	{
		//Used by the score keeper to detect if pipes have been passed
	}
}